// Copy to Config/Signing.local.xcconfig and enter the Team ID from developer.apple.com/account.
// DEVELOPMENT_TEAM = YOURTEAMID
// Change PRODUCT_BUNDLE_IDENTIFIER only before the first public release if Apple cannot register the legacy ID.
